import { Fragment } from 'react';
import { Grid, TextField } from '@mui/material';

const StudentIDForm = () => {
    return (
        <Fragment>
            <Grid>
                <TextField />
            </Grid>
        </Fragment>
    )
}

export default StudentIDForm;